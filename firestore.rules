rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 共通アカウントでログインしている場合のみ、すべての読み書きを許可
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
